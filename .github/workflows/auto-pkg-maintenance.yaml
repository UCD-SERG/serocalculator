# generated using:
# usethis::use_github_action(
#    url = "https://raw.githubusercontent.com/rstudio/education-workflows/main/examples/auto-pkg-maintenance.yaml"
#)

# notes: https://github.com/rstudio/education-workflows/tree/main/examples#auto-package-maintenance

on:
  # pull_request_target:
  #  types: [opened, synchronize, labeled]
  pull_request:
  push:
    branches: main

name: Package Maintenance

jobs:
  auto-pkg-maintenance:
    steps:
      - uses: rstudio/education-workflows/.github/workflows/auto-pkg-maintenance.yaml@HEAD
        with:
          extra-packages: moodymudskipper/devtag
