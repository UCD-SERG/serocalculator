% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_residuals.R
\name{calculate_residuals}
\alias{calculate_residuals}
\title{Calculate Residuals for Seroincidence Model}
\usage{
calculate_residuals(object, pop_data, standardize = TRUE, verbose = TRUE)
}
\arguments{
\item{object}{a \code{seroincidence} object from \code{\link[=est_seroincidence]{est_seroincidence()}}}

\item{pop_data}{the original population data used for fitting}

\item{standardize}{logical; if TRUE, return standardized residuals}

\item{verbose}{logical; if TRUE, print progress messages}
}
\value{
An object of class \code{"seroincidence_residuals"}, which is a tibble containing:
\itemize{
\item \code{age}: age of individual
\item \code{antigen_iso}: antigen-isotype measured
\item \code{observed}: observed antibody level
\item \code{predicted}: predicted antibody level from model
\item \code{residual}: observed - predicted
\item \code{standardized_residual}: residual / standard error
\item \code{se_estimate}: estimated standard error
}
}
\description{
Calculates residuals for a fitted seroincidence model by computing
predicted antibody levels for each observation and subtracting from
observed levels. Useful for model diagnostics and residual analysis.
}
\details{
Residuals are calculated as: observed - predicted

For standardization, residuals are divided by their estimated standard error.
The standard error is estimated from the noise parameters in the model.
}
\examples{
library(dplyr)

# Note: calculate_residuals() requires that model parameters be stored,
# which happens when fitting with cluster_var or stratification.
# This example is not run because the example data structure doesn't
# align with the parameter requirements of the residuals calculation.
\dontrun{
xs_data <- sees_pop_data_pk_100

curve <- typhoid_curves_nostrat_100 |>
  filter(antigen_iso \%in\% c("HlyE_IgA", "HlyE_IgG"))

noise <- example_noise_params_pk

# Fit model with clustering to store parameters
est <- est_seroincidence(
  pop_data = xs_data,
  sr_params = curve,
  noise_params = noise,
  antigen_isos = c("HlyE_IgG", "HlyE_IgA"),
  cluster_var = "cluster",
  iterlim = 5
)

# Calculate residuals
residuals <- calculate_residuals(
  object = est,
  pop_data = xs_data,
  standardize = TRUE
)
}
}
