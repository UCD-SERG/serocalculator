% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache_call.R
\name{.cache_call}
\alias{.cache_call}
\title{Call a function, caching its result on disk}
\usage{
.cache_call(
  fun,
  args,
  cache_path,
  cache_id,
  cache_rerun = FALSE,
  cache_verbose = TRUE,
  cache_extra = NULL,
  expected_class = NULL
)
}
\arguments{
\item{fun}{the \code{\link[=function]{function()}} to call.}

\item{args}{a \code{\link[=list]{list()}} of arguments to pass to \code{fun}.}

\item{cache_path}{a \code{\link[=character]{character()}} of length 1: the directory in which to
store cache files. A trailing slash is added if absent (see
\code{\link[=.validate_cache_path]{.validate_cache_path()}}). Results are written to \verb{cache_path/cache_id/}.}

\item{cache_id}{a \code{\link[=character]{character()}} of length 1: a stable identifier for this
cache entry. Distinct calls sharing a \code{cache_path} need distinct
\code{cache_id}s.}

\item{cache_rerun}{a \code{\link[=logical]{logical()}} of length 1: whether to discard any
existing cache and recompute. The freshly computed result is saved, so
later calls with \code{cache_rerun = FALSE} reuse it.}

\item{cache_verbose}{a \code{\link[=logical]{logical()}} of length 1: whether to report cache hits
and misses via \code{\link[cli:cli_inform]{cli::cli_inform()}}.}

\item{cache_extra}{an object folded into the cache key alongside the
arguments. \code{NULL} (the default) keys the cache on the arguments alone.
See the note on implementation changes in \code{\link[=sim_pop_data_multi_cached]{sim_pop_data_multi_cached()}}.}

\item{expected_class}{a \code{\link[=character]{character()}} of classes the result must inherit
from, or \code{NULL} to skip the check. Guards against a corrupted or
hand-edited cache file being returned as though it were a real result.}
}
\value{
the value of \code{do.call(fun, args)}, whether computed or loaded.
}
\description{
The shared engine behind \code{\link[=sim_pop_data_multi_cached]{sim_pop_data_multi_cached()}} and
\code{\link[=est_seroincidence_by_cached]{est_seroincidence_by_cached()}}. Wraps \code{\link[xfun:cache_exec]{xfun::cache_exec()}}, which keys the
cache on the \emph{values} of the free variables in the cached expression: here
that is \code{args}, so the cache is invalidated whenever the arguments change
and reused whenever they do not.
}
\details{
Every cache-control argument carries a \code{cache_} prefix because the wrapped
functions have their own \code{verbose} argument, which the wrappers must leave
free to pass through.
}
\keyword{internal}
