% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/f_dev_joint.R
\name{f_dev_joint}
\alias{f_dev_joint}
\title{Calculate the joint (shared latent time) negative log-likelihood
for several biomarkers}
\usage{
f_dev_joint(
  lambda,
  pop_data,
  curve_params,
  noise_params,
  antigen_isos,
  id_var = NULL,
  n_t_steps = 100L,
  ...
)
}
\arguments{
\item{lambda}{a \code{\link[=numeric]{numeric()}} vector of incidence parameters,
in events per person-year}

\item{pop_data}{cross-sectional data, either a \code{pop_data} \code{\link[=data.frame]{data.frame()}}
or a \code{\link[=list]{list()}} of them split by \code{antigen_iso}
(as \code{\link[=est_seroincidence]{est_seroincidence()}} supplies)}

\item{curve_params}{curve parameters in the units \code{\link[=f_dev]{f_dev()}} expects
(\code{alpha} per year, \code{d = r - 1}), or in their raw form (\code{alpha} per day
and \code{r}); either a \code{\link[=data.frame]{data.frame()}} or a \code{\link[=list]{list()}} split by \code{antigen_iso}}

\item{noise_params}{a \code{\link[=data.frame]{data.frame()}} or \code{\link[=list]{list()}} of noise parameters
(\code{nu}, \code{eps}, \code{y.low}, \code{y.high}) by \code{antigen_iso}}

\item{antigen_isos}{the biomarkers to combine}

\item{id_var}{name of the column in \code{pop_data} identifying subjects;
by default, read from the \code{id_var} attribute, falling back to a column
named \code{id} or \code{index_id}}

\item{n_t_steps}{number of midpoint-rule nodes for the numerical
integral over the latent infection time}

\item{...}{unused}
}
\value{
a \code{\link[=numeric]{numeric()}} vector of negative log-likelihoods,
corresponding to the elements of input \code{lambda}
}
\description{
Evaluates the negative log-likelihood of multi-biomarker cross-sectional
data under the model in which a person's biomarkers are conditionally
independent \emph{given} their time since infection, so that the per-biomarker
densities multiply inside one integral over that shared latent time.
This is the model the methodology article describes; the default
\code{method = "composite"} in \code{\link[=log_likelihood]{log_likelihood()}} instead sums the
per-biomarker marginal log-likelihoods, which gives each biomarker its
own latent time (see issues #637 and #646).
}
\details{
Subjects are paired across biomarkers by the id column of \code{pop_data}
(see \code{\link[=ids_varname]{ids_varname()}}); a subject may be missing some biomarkers.
Posterior draws of the curve parameters are paired across biomarkers
by \code{iter} (and \code{chain}, if present), or by position when
\code{curve_params} carries no \code{iter} column, so that a subject's biomarkers
are evaluated under one coherent draw. Every biomarker needs a noise
model (\code{nu > 0} or \code{eps > 0}); without noise a reading pins the latent
time exactly, and a joint density over several biomarkers is degenerate.

Interface with C routine \code{negloglik_joint} (\code{src/serocalc_joint.c}).
}
\keyword{internal}
