% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior_predictive_check.R
\name{posterior_predictive_check}
\alias{posterior_predictive_check}
\title{Posterior Predictive Check for Seroincidence Model}
\usage{
posterior_predictive_check(
  object,
  pop_data,
  n_sim = 100,
  verbose = TRUE,
  seed = NULL
)
}
\arguments{
\item{object}{a \code{seroincidence} object from \code{\link[=est_seroincidence]{est_seroincidence()}}}

\item{pop_data}{the original population data used for fitting}

\item{n_sim}{number of simulated datasets to generate}

\item{verbose}{logical; if TRUE, print progress messages}

\item{seed}{random seed for reproducibility}
}
\value{
An object of class \code{"posterior_predictive_check"}, which is a list containing:
\itemize{
\item \code{observed}: the original population data
\item \code{simulated}: a list of n_sim simulated datasets
\item \code{observed_summary}: summary statistics for the observed data
\item \code{simulated_summary}: summary statistics for each simulated dataset
\item \code{lambda_estimate}: the estimated incidence rate
\item \code{antigen_isos}: antigen-isotypes included in the analysis
\item \code{n_sim}: number of simulations
}
}
\description{
Performs a posterior predictive check (PPC) by simulating datasets from
a fitted seroincidence model and comparing them to the observed data.
This helps assess whether the fitted model adequately reproduces the
observed data patterns.
}
\details{
The posterior predictive check works as follows:
\enumerate{
\item Extracts the estimated incidence rate (lambda) from the fitted model
\item Simulates n_sim datasets using the fitted lambda and model parameters
\item Computes summary statistics for observed and simulated data
\item Returns an object containing both data and summaries for visualization
}
}
\examples{
library(dplyr)

xs_data <- sees_pop_data_pk_100

curve <- typhoid_curves_nostrat_100 |>
  filter(antigen_iso \%in\% c("HlyE_IgA", "HlyE_IgG"))

noise <- example_noise_params_pk

# Fit model
est <- est_seroincidence(
  pop_data = xs_data,
  sr_params = curve,
  noise_params = noise,
  antigen_isos = c("HlyE_IgG", "HlyE_IgA")
)

# Perform posterior predictive check
\donttest{
ppc <- posterior_predictive_check(
  object = est,
  pop_data = xs_data,
  n_sim = 100
)
}
}
