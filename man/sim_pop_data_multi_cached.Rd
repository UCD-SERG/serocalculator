% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_pop_data_multi_cached.R
\name{sim_pop_data_multi_cached}
\alias{sim_pop_data_multi_cached}
\title{Simulate multiple data sets, caching the result on disk}
\usage{
sim_pop_data_multi_cached(
  ...,
  cache_path = "cache/",
  cache_id = "sim_pop_data_multi",
  cache_rerun = FALSE,
  cache_verbose = TRUE,
  cache_extra = NULL
)
}
\arguments{
\item{...}{
  Arguments passed on to \code{\link{sim_pop_data_multi}}
  \describe{
    \item{\code{nclus}}{number of clusters}
    \item{\code{rng_seed}}{starting seed for random number generator,
passed to \code{\link[rngtools:RNGseq]{rngtools::RNGseq()}}}
    \item{\code{sample_sizes}}{sample sizes to simulate}
    \item{\code{lambdas}}{incidence rate, in events/person*year}
    \item{\code{num_cores}}{number of cores to use for parallel computations}
    \item{\code{verbose}}{whether to report verbose information}
    \item{\code{sim_function}}{which function to use: \code{\link[=sim_pop_data]{sim_pop_data()}} or \code{\link[=sim_pop_data_2]{sim_pop_data_2()}}}
  }}

\item{cache_path}{a \code{\link[=character]{character()}} of length 1: the directory in which to
store cache files. A trailing slash is added if absent (see
\code{\link[=.validate_cache_path]{.validate_cache_path()}}). Results are written to \verb{cache_path/cache_id/}.}

\item{cache_id}{a \code{\link[=character]{character()}} of length 1: a stable identifier for this
cache entry. Distinct calls sharing a \code{cache_path} need distinct
\code{cache_id}s.}

\item{cache_rerun}{a \code{\link[=logical]{logical()}} of length 1: whether to discard any
existing cache and recompute. The freshly computed result is saved, so
later calls with \code{cache_rerun = FALSE} reuse it.}

\item{cache_verbose}{a \code{\link[=logical]{logical()}} of length 1: whether to report cache hits
and misses via \code{\link[cli:cli_inform]{cli::cli_inform()}}.}

\item{cache_extra}{an object folded into the cache key alongside the
arguments. \code{NULL} (the default) keys the cache on the arguments alone.
See the note on implementation changes in \code{\link[=sim_pop_data_multi_cached]{sim_pop_data_multi_cached()}}.}
}
\value{
a \code{\link[tibble:tibble]{tibble::tibble()}}, as returned by \code{\link[=sim_pop_data_multi]{sim_pop_data_multi()}}.
}
\description{
A caching wrapper around \code{\link[=sim_pop_data_multi]{sim_pop_data_multi()}}, which dominates the runtime
of most simulation studies. The first call simulates and saves; later calls
with the same arguments load the saved result instead of re-simulating.
}
\details{
The cache is keyed on the \strong{arguments}, so changing any of them (a new
\code{lambdas} vector, a different \code{nclus}) recomputes automatically, and
restoring the old arguments reuses the old result. Argument order and
formatting do not matter.

The cache is \emph{not} keyed on the implementation. If \code{sim_pop_data_multi()}
or the functions it calls change, an existing cache stays valid as far as
this wrapper is concerned. Pass \code{cache_rerun = TRUE} once to refresh it, or
fold a version into the key with, for example,
\code{cache_extra = utils::packageVersion("serocalculator")}. That is
deliberately not the default: a development version bumps often, which
would discard the cache on nearly every commit.
}
\examples{

antibodies <- c("HlyE_IgA", "HlyE_IgG")

# Examples must not write into the user's working directory, so send the
# cache to a temporary one. `cache_path` gains its trailing slash
# automatically.
cache_dir <- file.path(tempdir(), "serocalculator-example-cache")

sim_df <- sim_pop_data_multi_cached(
  curve_params = typhoid_curves_nostrat_100,
  lambdas = c(0.05, 0.1),
  nclus = 2,
  sample_sizes = 50,
  age_range = c(0, 10),
  antigen_isos = antibodies,
  num_cores = 1,
  add_noise = TRUE,
  noise_limits = rbind(
    HlyE_IgA = c(min = 0, max = 0.5),
    HlyE_IgG = c(min = 0, max = 0.5)
  ),
  format = "long",
  cache_path = cache_dir
)

nrow(sim_df)

# Calling again with the same arguments loads the saved result instead of
# re-simulating.
sim_df2 <- sim_pop_data_multi_cached(
  curve_params = typhoid_curves_nostrat_100,
  lambdas = c(0.05, 0.1),
  nclus = 2,
  sample_sizes = 50,
  age_range = c(0, 10),
  antigen_isos = antibodies,
  num_cores = 1,
  add_noise = TRUE,
  noise_limits = rbind(
    HlyE_IgA = c(min = 0, max = 0.5),
    HlyE_IgG = c(min = 0, max = 0.5)
  ),
  format = "long",
  cache_path = cache_dir
)

identical(sim_df, sim_df2)
}
\seealso{
\code{\link[=est_seroincidence_by_cached]{est_seroincidence_by_cached()}}
}
