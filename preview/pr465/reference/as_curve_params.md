# Load antibody decay curve parameter

**\[deprecated\]**

`as_curve_params()` was renamed to
[`as_sr_params()`](https:/ucd-serg.github.io/serocalculator/preview/pr465/reference/as_sr_params.md)
to create a more consistent API.

## Usage

``` r
as_curve_params(...)
```
