# Simulate a cross-sectional serosurvey with noise

**\[deprecated\]**

`sim.cs()` was renamed to
[`sim_pop_data()`](https:/ucd-serg.github.io/serocalculator/preview/pr465/reference/sim_pop_data.md)
to create a more consistent API.

## Usage

``` r
sim.cs(n.smpl, age.rng, n.mc, renew.params, add.noise, ...)
```
