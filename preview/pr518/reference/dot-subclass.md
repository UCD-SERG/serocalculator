# Prepend a subclass to an object's class vector

Prepend a subclass to an object's class vector

## Usage

``` r
.subclass(x, subclass)
```

## Arguments

- x:

  An object.

- subclass:

  Character string to prepend to the class vector.

## Value

`x` with `subclass` prepended to its class.
